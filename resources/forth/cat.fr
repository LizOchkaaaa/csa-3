begin
    clear       /очистка стека после каждой итерации, чтобы не засорять стек данных/
    key         /получение символа из буфера ввода/
    0 != if drop drop emit      /если символ не 0, то печатаю его/
    else drop ;
0 != until          /если пришел 0-терминатор с ввода, то значит, что строка закончилась/